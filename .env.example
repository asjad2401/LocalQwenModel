# Copy to .env and fill in keys you want to use.
# The agent reads these via the config.yaml api_key fields,
# or you can set them as env vars and reference them in config.yaml.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
